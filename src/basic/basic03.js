/* 
  Реализовать функцию, которая принимает три числа и возвращет true если они равны, и false в противном случае.
*/
// Функция проверяет равенство всех трёх чисел
export function areEqual(a, b, c) {
    return a === b && b === c
}


